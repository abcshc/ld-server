export default class LoginUserInformation {
  nickname: string;
  imageUrl: string;

  constructor(nickname: string, imageUrl: string) {
    this.nickname = nickname;
    this.imageUrl = imageUrl;
  }
}
